- remove CWD from content?
- format output
  - multiple globs
  - symlinks
  - flags
    x headings on/off (--no-headings)
    x filters by type
      x -f / --files
      x -d / --dirs
      x -s / --symlinks
      x -e / --exes
    x --no-colors
    x --no-columns
    - --no-types
    - appended symbols on/off (--no-symbols)
    - --no-format (no-columns, no-colors, no-symbols, no-types)
    - customizable colors
    - file size
    - edit time

- fix bug when pointing up (e.g. '..')
- change amount of res removed from CWD to CWD + pre-glob
- confirm proper error handling in all co-wrapped generator fns
- combined depth search (eg test/**/* -x finds all executables in tree)
- write help text
- debug lib support
- update notifier support

- readme
- tests
- docstrings
- ci
- npm

- maybe later
  - customizable headers
  - customizable symbols (exe, dir)
  - customizable output sorting
