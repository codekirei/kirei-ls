- format output
  - DRY parseContent (parseX -> types.map(type => parseAr(type)))
  - multiple globs
  - symlinks
  - flags
    x headings on/off (--no-headings)
    - unformatted, single column output
    - colors on/off (--no-colors)
    - customizable colors
    - symbols on/off (--no-symbols)
    - customizable symbols
    - file size
    - edit time
    - filters by type (only show files, etc)

- confirm proper error handling in all co-wrapped generator fns
- combined depth search (eg test/**/* -x finds all executables in tree)
- write help text
- debug lib support
- update notifier support

- readme
- tests
- docstrings
- ci
- npm

- maybe later
  - customizable headers
  - customizable output sorting
